% Import all packages needed for the document.
\usepackage{misc/packages}

% Make it possible to make specific text parts narrower.
\newenvironment{narrowsection}[2]
{
	\ifthenelse{\equal{#2}{left}}
	{
		% Left aligned.
		\begin{adjustwidth}{0mm}{\dimexpr(210mm - #1)\relax}
	}
	{
		% Use center aligned as default.
		\begin{adjustwidth}{\dimexpr(210mm - #1)/2\relax}{\dimexpr(210mm - #1)/2\relax}
	}
}
{\end{adjustwidth}}

% For nested quotation marks.
\newcommand{\hsp}{\kern 1pt}

% Paragraph formatting.
\renewcommand{\baselinestretch}{1.125} % Allow paragraphs to breathe by spreading the lines further.
\setlength{\parskip}{0pt}              % Fixed space between paragraphs (i.e. disable variable parskip).
\setlength{\parindent}{1em}

% Don’t add extra space after sentences.
\frenchspacing

% Reduce widows/orphans.
\widowpenalty=10000
\clubpenalty=10000

% Page size
\usepackage[
    a4paper,
    total  ={160mm, 250mm},
    top    =30mm,
    bottom =30mm,
    outer  =20mm,
    inner  =20mm
]{geometry}

% Reduce overfull \hbox{} warnings.
\sloppy

% Set the behaviour of the columns.
\setlength\columnsep{20pt} % This is the default columnsep for all pages.

% Contents page.
\renewcommand{\cftchapfont}{\large\itshape}
\renewcommand{\cftchappagefont}{\normalfont}
\renewcommand{\numberline}[1]{}

% Chapter headings.
\renewcommand\thechapter{\Roman{chapter}}

% Headers / Footers.
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhf[CFE,CFO]{\thepage}          % Set page numbers in the footer.
%\fancyhead[LE,RO]{\thepage}         % Set page numbers in the left/right sides of the header.
\fancyhead[CE]{\itshape\chaptername} % Chapter name in middle of left-side page header.
\fancyhead[CO]{\itshape\booktitle}   % Book title in middle of right-side page header.
